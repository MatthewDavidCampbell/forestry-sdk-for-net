# Release History

## 1.0.0-beta.1 (Unreleased)

### Features Added
- `Identity` code documentation along side more robust test cases is hopefully self-explanitory.
- End-users define a `Profile` to create instances of `Identity`

### Other Changes
- TODO about prefix and suffix policies rather than hard-coding logic (i.e. plug-in-play options)