# Basics
The documentation under this directory is divided into categories:

- dev
- design

# Development
Under ```dev``` the focus is on how the SDK libraries are each a project SDK with MSBuild targets and associated tasks to compile, package and publish code.

# Design
Design goals generic to SDK libraries shape things like:

- constructor accessibility
- required slash optional properties
- how collections are defined

